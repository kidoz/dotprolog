[project]
name = "dotprolog-docs"
version = "0.0.0"
description = "Documentation toolchain for DotProlog."
requires-python = ">=3.14,<3.15"
dependencies = []

[dependency-groups]
docs = [
    "mkdocs==1.6.1",
]

[tool.uv]
default-groups = ["docs"]
package = false
required-version = "==0.12.0"
