<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <RootNamespace>Prolog.CodeGen.CSharp.Tests</RootNamespace>
    </PropertyGroup>

    <ItemGroup>
        <!-- The generated facade is compiled in-process, so a test proves it builds and runs
             rather than only asserting on the text. -->
        <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="4.14.0" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\..\src\Prolog.CodeGen.CSharp\Prolog.CodeGen.CSharp.csproj" />
    </ItemGroup>
</Project>
